using EntityFramework.Entities;
using Microsoft.AspNetCore.Identity;
using System.Collections.Generic;
using System.Security.Claims;
using System.Threading.Tasks;

namespace EntityFramework.Repositories.Interfaces
{
    public interface IIdentityRepository
    {
        //Task<(IdentityResult identityResult, string userId)> RegisterAsync(string email, string password);
        //Task<(Employee user, bool result)> LoginAsync(string email, string password);
        //Task<IList<Claim>> GetUserClaimAsync(Employee user);
        //Task<IList<string>> GetUserRoles(Employee user);
        //Task<IdentityRole> FindRoleByNameAsync(string role);
        //Task<IList<Claim>> GetRoleClaimsAsync(IdentityRole role);
        //Task<Employee> GetUserByIdAsync(string userId);
    }
}
